# Сигнификация — архитектура сайта

Студия 3D-визуализации интерьеров и экстерьеров. Цель: programmatic SEO на повторяющихся паттернах запросов + генератор картинок как лидмагнит. Сейчас фиксируем архитектуру, не вёрстку.

Имя репозитория на GitHub пока `CRM-AI`. Сайт в проде — отдельный slug, не `crm-ai`.

## 1. Что продаём на самом деле

Не «ещё один Midjourney». Продукт — визуализация по плану: ракурсы → правки в утверждённой сцене → финальные кадры для согласования. Не дизайн-проект под ключ. Не картинка из ИИ. Не 350 ₽/м².

Генератор — фаза 4, превью и крючок. В пилоте его нет. Деньги: заказ студии. Подписка на генерации — до ent, не ядро: иначе конкурируем с Flux.

**Два входа, не четыре персоны.** Частник — дефолт всей IA. Дизайнер — `/dlya-dizaynerov` в пилоте, не «если канал». Застройщик — страница после живого кейса ЖК, не в шапке. Риелтору страницы нет. Пришёл — розничный пакет, без опта «на 20 объявлений».

| Кто | Покупает | CTA |
| --- | --- | --- |
| Частник | пакет ракурсов на согласование | расчёт по планировке |
| Дизайнер | мощность, правки, сцена, NDA, SLA, white-label | Telegram + файл, не «мы перезвоним» |
| Застройщик | later | нет в меню |
| Риелтор | не персона | нет страницы |

План, который принёс дизайнер, частнику через `/uslugi/varianty` не продаём. Иначе white-label сдохнет на втором заказе.

**Оффер частнику:** N ракурсов, 2 правки в утверждённой сцене, срок, кадры для согласования.

**Оффер дизайнеру:** те же пакеты письмом (net не на `/ceny`), сцена и исходники по договору, NDA, SLA. Клиенту не пишем.

Цена: нижняя полка московских студий по качеству кадра, чернозёмная себестоимость. Моат не в дешевизне. На `/ceny` — пакеты 3 / 6 / 10 + фасад отдельно. ₽/м² — сноска (ориентир 1 200–2 000), не H1. «От 350/500/800» не публикуем. Блок «почему не Авито» обязателен. Эконом-полка и `E2` на агрегаторах не заводим: карточка Профи/Авито = `R3` или «по запросу».

**Не писать в коммерции:** бесплатно, онлайн, нейросеть, как Midjourney, от 350 ₽/м², дизайн под ключ, для риелторов, филиал Москва без адреса. «ИИ» — не в title услуг и не в первом экране `/`.

## 2. Жёсткие правила pSEO (из практики + Toolfox)

Яндекс режет шаблон сильнее Google. «1 000 страниц → топ» без данных — фильтр, не стратегия.

- Страница существует только если есть спрос **и** уникальные данные (свои рендеры + поля, которых нет у соседа).
- В индексе: хабы и белый список комбинаций. Остальные фильтры — `noindex, follow` или query без ЧПУ.
- Не публиковать всё сразу. Пилот → метрики → волны.
- 70% уникальности — не синонимайз. Это доля **уникальных блоков данных** на странице (рендеры, метрики объекта, решения, палитра, FAQ кластера). Общий каркас 10–15%.
- Нет данных — нет URL. Пустую комбинацию не создаём.
- Раз в квартал вырезать страницы без трафика и без охвата.

Пилот: 40–80 URL, не 1 000.

## 3. Карта сайта: четыре слоя, не каша

```
Коммерция (до любых комбо)     Хабы
/                              /stili  /stili/{style}
/o-studii /process /ceny       /komnaty /komnaty/{room}
/uslugi/{slug}                 /eksterier /eksterier/{style}
/dlya-dizaynerov /kontakty /legal/*

Комбо (только style×room)      Каталог
/stili/{style}/{room}          /raboty              index, self-canonical
                               /raboty/{project}    index только с паспортом
                               /lk /api             noindex
```

Нет `/obekty/*` и нет `/stili/{style}/{object}` в v1. «Квартира в стиле лофт» закрывает хаб `/stili/loft`. «Визуализация дома / фасада» — `/uslugi/vizualizaciya-doma` и `/eksterier/{style}`.

«Кухня в стиле лофт» — только `/stili/loft/kuhnya`. Фильтры каталога не существуют как `<a href="?…">`: в HTML только белые ЧПУ. Query, если появится, — `noindex` + canonical на хаб/комбо/`/raboty`. page≥2 — noindex или load-more без URL.

Сравнения стилей (`/sravneniya/loft-vs-skandi`) — вторая очередь, не пилот.

Блог — не pSEO. 8–15 экспертных опор (процесс, сроки, бриф, ошибки заказчика). Их пишем вручную.

## 3.1 Коммерческие услуги (есть до любой тысячи URL)

| URL | Что |
| --- | --- |
| `/uslugi/vizualizaciya-interiera` | Интерьер: пакеты 3 / 6 / 10 |
| `/uslugi/vizualizaciya-fasada` | Свой пакет и срок, не «как кухня» |
| `/uslugi/vizualizaciya-doma` | Свой пакет, не `/stili/x/dom` |
| `/uslugi/render-planirovki` | По планировке / обмерному плану |
| `/uslugi/varianty` | Ровно 2 стиля × 3 ракурса одного плана. Не подбор мебели. Если план уже у дизайнера — через него |
| `/dlya-dizaynerov` | Корень, не `/uslugi/*`. SLA, NDA, сцена, white-label |
| `/dlya-zastroyshchikov` | После кейса ЖК, не в шапке |
| офис / ритейл / новостройка | В таксономии later. В меню пилота нет |
| анимация | Нет в меню пилота |

Цены: `R3` / `R6` / `R10` на `/ceny` + `RF` на страницах фасада/дома. 2 правки = камера / свет / материал в **той же** утверждённой модели. Смена стиля, мебели, плана или правка чужой сцены — новый счёт. Сцена / EXR — доплата. Правка сверх пакета — строка. Коды: `docs/gtm.md` §7. `E2` не заводим.

## 3.2 Приёмка, референс, права (обязательные абзацы)

На `/process` и в оферте, не «потом»:

- Референс (нейросеть, Pinterest, наше портфолио) задаёт направление, не номенклатуру и не геометрию.
- 2 правки = итерации в утверждённой модели. Новый стиль / мебель / план = новый счёт.
- Приёмка — письменное «ок» по списку ракурсов.
- Бренды в кадре — визуал, не продажа мебели и не офер вендора.
- Сцена принадлежит студии, пока не выкуплена. Архив 12 месяцев, потом удаление.
- ИИ-превью, если появится, не результат по договору. Подпись на кадре: «превью, не рендер студии».
- White-label: отдельный B2B-договор. Кадр дизайнера его клиенту — как свои. Этот клиент за «бесплатными правками» к нам не ходит.

Хабы и комбо: первый абзац — «кадры студии по реальным планам, не онлайн-конструктор и не каталог мебели». CTA «расчёт по вашей планировке». Кнопки «сгенерировать» нет. FAQ: пакет, правки, срок, форматы — не «что такое лофт».

## 4. Типы шаблонов

| Тип | URL | Зачем | Индекс |
| --- | --- | --- | --- |
| Главная | `/` | Два входа (квартира/дом и дизайнер), пакеты, 8 studio-кадров. Генератора в первом экране нет | да |
| О студии | `/o-studii` | E-E-A-T, люди, софт, процесс | да |
| Процесс | `/process` | Референс ≠ ТЗ, правки, приёмка, сцена | да |
| Услуга | `/uslugi/*` | Интерьер, фасад, дом, план, варианты. Без анимации в меню | да |
| Дизайнерам | `/dlya-dizaynerov` | Второй оффер, не SEO-хаб | да |
| Цены | `/ceny` | Три пакета, что входит / не входит, почему не Авито. Якоря `#paket-3` | да |
| Хаб стиля | `/stili/loft` | Коммерция: визуализация в стиле X, закрывает и «X квартира» | да |
| Хаб комнаты | `/komnaty/kuhnya` | «визуализация кухни» без стиля | да |
| Хаб экстерьера | `/eksterier/barnhaus` | Фасад/дом снаружи, не интерьер | да |
| Комбо | `/stili/loft/kuhnya` | Только style×room из белого списка | да |
| Каталог | `/raboty` | Витрина, фильтр без смены индексного URL | да |
| Проект | `/raboty/{slug}` | Кейс с паспортом; H1 объекта, не «кухня лофт» | условно |
| ЛК / API | `/lk`, `/api` | Генерации, заявка | нет |

Хаб стиля — не Wikipedia (InMyRoom не обгоним справочником). H1: «Визуализация интерьера в стиле {С}». Комбо: «{Комната} в стиле {С}». Два URL с «кухня»+«лофт» в title запрещены.

Формулы title/H1 — `docs/taxonomy.md` §9. Без них каннибал родится в шаблоне.

## 5. Таксономия: оси, не бесконечный декартов продукт

Полный справочник — `docs/taxonomy.md`. Здесь правила.

**Индексируем оси по отдельности и только белые пары.** Не `16 стилей × 12 комнат × 6 объектов × 10 городов`.

Словарь стилей интерьера (must, **не** все сидировать в пилот): современный, минимализм, сканди, лофт, классика, неоклассика, прованс, ар-деко, джапанди, хай-тек. Second-tier: эко, бохо, средиземноморский, mid-century, английский, американская классика, кантри, эклектика, японский, восток. Пилот: 6–8 хабов, по которым уже есть серии кадров.

Синонимы → 301, таблица `Redirect`. `modern` не сидировать: бытовой «модерн» → `sovremennyj`.

Экстерьер — своё дерево, ключ стиля `(domain, slug)`: `domain ∈ {interior, exterior}`. Одинаковый `sovremennyj` допустим в двух деревьях, H1 всегда с «интерьер» или «фасад». Барнхаус и шале в пилоте — только `/eksterier/*`.

Комнаты-хабы пилота: кухня, кухня-гостиная, гостиная, спальня, детская, ванная. Прихожая/кабинет/гардеробная/балкон — хабы без style×room на старте. Зал→гостиная, лоджия→балкон, коридор→прихожая. Санузел в пилоте alias на ванную.

Объект (`kvartira`, `dom`, `studiya`) — атрибут проекта, не ось URL. Офис/HoReCa — услуги.

Гео: город в title коммерции и `/kontakty` + один `ProfessionalService`. Отдельный `/moskva` не обязателен. Районы и город×фасет — запрет.

KPI — не «1000 URL» и не 600 карточек в индексе. После пилота: коммерция + 6–8 стилей + 5–6 комнат + 2–3 фасада + 8–15 комбо + **20–40** индексных проектов с паспортом. Остальные работы в каталоге с `indexable=false`. Масса листьев — только после того, как хабы в индексе и нет всплеска «малоценных».

## 6. Модель данных (страница = запись, не HTML-файл)

Каждый индексный URL — сущность в CMS/БД. Генератор HTML только читает.

```
Style          UNIQUE (domain, slug)   domain = interior | exterior
               aliases[], tier, unique_blocks, faq[]

Room           slug, unique_intro, faq[], index_policy
ObjectType     slug, domain, index_policy
               -- interior: kvartira, dom, studiya  (атрибут проекта, не path)
               -- exterior: fasad, uchastok         (не под /stili)

Service        5–6 статических /uslugi/*, не ось комбо
Redirect       from_path, to_path, 301|410
Image          project_id, room_id?, origin, alt, caption, sort, path
UrlRegistry    path UNIQUE, entity, index_policy, lastmod   -- SSG/sitemap

Project        domain, primary_style_id, extra_style_ids[],
               room_ids[], primary_room_id, object_id,
               area_m2, year, city? (не в title/h1), house_series?,
               brief, decisions[], palette[], materials[],
               cover_image_id, status, index_policy, noindex_reason
               -- related и сетка комбо СЧИТАЮТСЯ, не related_ids[] / project_ids[]

LandingCombo   style_id + room_id only
               h1, title, meta — ручные
               pinned[] / excluded[] поверх query(Project)
               index только если wordstat_at + intro + ≥4 studio-кадра ЭТОЙ пары

-- GenerationJob / CreditLedger не создавать до фазы генератора
Lead           форма заявки, без кредитов
```

Инварианты в миграции: `Room.slug ∩ ObjectType.slug = ∅`; комбо только `Style.domain=interior`; проект `index=true` при ≥3–4 `origin=studio` + паспорт; SSG читает только `UrlRegistry`.

`city` / район / серия дома — уникальность карточки, не ЧПУ.

## 7. Перелинковка

- Хаб стиля → белые комнаты + проекты + 2 **семантических** соседа (лофт↔хай-тек, сканди↔джапанди), не алфавит.
- Комбо → хаб стиля, хаб комнаты, тот же room в другом стиле. «6 соседей» на пилоте пустые — фолбэк на хабы.
- Хлебные комбо: Главная → Стили → Лофт → Кухня в стиле лофт. Не второй путь через `/komnaty`.
- Проект → стиль, primary room, 3 соседа. Title проекта без «в стиле X» первым.
- Мёртвое комбо: 301 на ближайший хаб, запись в `Redirect`, выкинуть из sitemap. Не пачка 404.
- Sitemap из `UrlRegistry`: только 200 + self-canonical + index. `lastmod` = дата контента. Всплеск «малоценных» — следующую волну не лить.

## 8. Генератор — виджет, не контент страницы

Контекст страницы (стиль, комната) идёт в промпт. Результат не индексируется и не становится URL.

Виджет (фаза 4) только на `/` и `/uslugi/*`, ниже текста. **Не** на `/stili/*`, `/komnaty/*`, комбо, `/legal/*`, `/kontakty`, `/ceny`. Комбо кормят «фото/купить/онлайн» — кнопка «сгенерировать» там убивает интент. `/generator` если будет — `noindex`. Запрос с «бесплатно|онлайн|нейросеть|конструктор» — 0 кредитов, не GPU.

Бриф «2 + 6 и полный файл после телефона» отвергнут: SEO-трафик сожжёт GPU, полный кадр за номер убивает заявку.

### Воронка кредитов

| Состояние | Кредиты | Что можно |
| --- | --- | --- |
| Аноним | 1 / 30 дней | квиз или короткий промпт; водяной знак; ≤1024; без download |
| Телефон + OTP | +2, сгорают за 14 дней | история в ЛК, знак **остаётся**, merge гостевого job |
| Пакет 20/50 | только в ЛК после лимита | полный файл, без знака. Не на `/ceny` студии |
| Заявка студии | 0, отдельный поток | человек, счёт, договор. Превью = референс, не ТЗ |

Вес: текст/квиз = 1, планировка (img2img) = 3. Один job = один кадр. Fail провайдера кредит не жжёт.

Квиз важнее свободного промпта: размер / палитра / материалы / свет, плюс опциональный план. В `GenerationJob` хранить `seed` и системный negative. Готово — пуш в Telegram, если пользователь так просил (это доставка, не Login).

После 1-й генерации сразу два CTA: «просчёт студии» и «ещё превью — телефон». Кредитная модалка ≠ заявка. Согласия разные: аккаунт, контакт студии, реклама, трансграничка GPU.

Потолок: ≤4 бесплатных кадра на `phone_e164` / 30 дней, включая смерженные гостевые. Один телефон — один ledger. Удалил аккаунт — номер в блоклисте грантов.

Пакеты не в коммерческом ядре. «Человеческий тариф» в кредитах не заводим. Telegram — поле в заявке, не Login.

### Антиабьюз и API

- Квота гостя: signed httpOnly `guest_id` + мягкий `ip_hash`. Fingerprint не собираем.
- Лок квоты до enqueue, `idempotency_key`. Капча со 2-й генерации и всегда на OTP.
- OTP: согласие до отправки SMS. 3/телефон/сутки, 5/IP/час. Поручение шлюзу. Не-RU — только явным решением. Стоп-лист виртуальных номеров.
- Дневной `gpu_budget`: 80% — стоп гостей; 100% — стоп всех кроме заявки студии. Гостю дешёвая модель.
- Планировка: MIME/size, не публичный CDN, TTL 30 дней, «имею право», не в training, strip EXIF.
- `GET /jobs/:id/download` — 402 без `paid | studio_contract`.
- Гостевые job мержатся в ЛК атомарно, квота не удваивается.
- User-gen в каталог в v1 не публикуем.

Интерьер по плану ≈ $0.04–0.20 за удачный кадр. Юнит до кода: `$/текст`, `$/план`, ₽ SMS, визит→ген→OTP→заявка→счёт. Если `trial_cost × tryers > 0.3 × чек × CR` — виджет уже, не шире.

### Чтобы не убить SEO / CWV

- Остров: `import()` по idle / IO / click. SSR без модели и WebGL.
- `/api/generate` за капчей/сессией, ключ на бэке, `noindex`.
- Результат: `/lk/jobs/{id}`, не sitemap.
- Дисклеймер в виджете: ИИ-превью ≠ рендер студии.
- Не обещать в H1 «магия по планировке».

## 9. Юридика и доверие (обязательный минимум)

Политика в футере — не контур. Жёсткий разбор: `docs/architecture-audit.md`.

- Уведомление в РКН как оператор (ст. 22) **до** первого телефона.
- Политика + оферта + отдельное согласие. Чекбокс пустой. Лог: timestamp, IP, UA, версия текста, id формы.
- С 01.09.2025 нельзя склеивать ПДн + рекламу/звонок/SMS в один чекбокс (38-ФЗ ст. 18). OTP — транзакционное; «мы перезвоним» — реклама, отдельное согласие.
- **Локализация 152-ФЗ (с 01.07.2025):** первичный сбор/запись ПДн граждан РФ — на серверах в РФ. Аккаунты не в Supabase EU / Vercel KV / US S3 как единственном сторе.
- Трансграничка (fal.ai / Replicate / GA4): отдельное согласие + уведомление РКН **до** передачи. Планировка = потенциальный адрес/серия дома — в цели обработки явно. Штрафы юрлицу до 6 / 18 млн.
- Хостинг ПДн и ЛК: РФ (Selectel / Yandex Cloud / контур t9). GPU за рубежом — только как обработчик с ДПДн и оформленной ТППДн, либо свой GPU в РФ.
- Планировки: срок хранения, удаление по запросу, strip EXIF GPS либо явное согласие на гео.
- 54-ФЗ на подписку/пакеты: чек (ЮKassa «Чеки» или своя касса), оферта, автопродление, отмена в 1 клик.
- Реквизиты и контакты на `/o-studii` и в подвале. Дисклеймер: ИИ-превью ≠ финальный рендер.
- Рендеры студии vs витринные нейрокадры — флаг `origin: studio | campaign`. Не мешать в одном списке.

## 10. Стек (чтобы потом не переписывать URL)

- Публичные страницы: SSG (Astro или Next.js). 1 000 URL должны быть файлами/пререндером, не SSR из пустой CMS на каждый хит.
- CMS: Postgres + админка (Payload или своя тонкая). Не Google Sheets в проде.
- Генерации: очередь (Redis + worker), провайдер картинок за адаптером (`ImageGenProvider`).
- Auth: Telegram Login **или** OTP (SMS.ru / smsc) **или** email. Сессия. ЛК — те же origin, `noindex`.
- Метрика: Яндекс.Метрика по умолчанию. GA4 — только после согласия и с пониманием трансгранички.
- Картинки портфолио: AVIF/WebP, srcset, свой CDN/nginx, image sitemap.
- Поиск/мониторинг: Вебмастер + GSC + Topvisor/Rush на белый список URL, не на «все query».
- Хост: t9, slug/порт — в `docs/deploy.md` когда дойдём до деплоя, не наугад.

Стек пилота закрыт: **Next.js SSG + Payload + Postgres**, один origin для сайта, `admin.` для CMS. Не «Astro или Next». Rebuild по webhook `published`. Картинки через сущность `Image` + деривативы WebP/AVIF, не файлы в `/public` руками. Staging = тот же билд, `noindex` на хост.

Схема: главная/контакты — `ProfessionalService` + `areaServed` (один город). Услуга — `Service` + `Offer`. Хаб/комбо — `CollectionPage` + `BreadcrumbList`. `FAQPage` только если FAQ уникален и в HTML. Проект — `CreativeWork` + `ImageObject[]`. Не `Offer` без цены на комбо. Не `LocalBusiness` в N городах.

## 11. Что в исходном брифе было слабо и как усилили

1. **Декарт фильтров в индекс** — заменён белым списком и раздельными шаблонами.
2. **1 000 URL день в день** — пилот, потом волны, потом вырезание.
3. **70% уникальности без модели данных** — уникальность = заполненные поля + свои кадры.
4. **Генератор как ядро** — генератор как лидмагнит, оффер студии на каждой странице.
5. **Экстерьер потерялся бы в «комнатах»** — отдельное дерево.
6. **Гео не оговорено** — максимум одна локальная страница.
7. **Нет коммерческого ядра** — услуги, цены, процесс, контакты обязательны до массы pSEO.
8. **Телефон без 152-ФЗ и антиабьюза** — OTP с лимитами, раздельные согласия, один ledger на номер.
9. **Каннибализация** «лофт» vs «кухня лофт» vs карточка проекта — разные шаблоны и одна каноника.
10. **Нет прунинга** — страница без данных или без спроса не живёт в индексе.
11. **Витрина из одной нейросети** без живых кейсов — слабый E-E-A-T. В первую волну кладём реальные работы, даже если их 20.
12. **2+6 и полный файл за номер** — отвергнуто: 1+2, знак не снимается, download только пакет/договор.
13. **Подписка на `/ceny`** — пакеты только в ЛК, не рядом с договором студии.
14. **style×object и `/obekty`** — выпилены: каннибал хаба стиля и дерева экстерьера.
15. **600–800 индексных карточек** — не KPI. В индекс только проекты с паспортом.
16. **Один оффер на всех** — два входа: частник и дизайнер. Риелтор вычеркнут. Застройщик не в шапке.
17. **Цена «от» без полки** — пакеты 3/6/10, не ₽/м² в H1, не эконом 350–800.
18. **Правки без протокола** — референс ≠ ТЗ, 2 итерации в утверждённой модели, сцена не «в подарок».

## 12. Публикация, robots, гейт качества

`robots.txt`: закрыть `/lk`, `/api`, query-строки каталога не отдаём в sitemap. Не закрывать ЧПУ комбо.

Sitemap: `sitemap-core.xml`, `sitemap-hubs.xml`, `sitemap-combos.xml`, `sitemap-projects.xml`, `sitemap-images.xml`. Новый URL в индекс — только из волны, не «все draft разом».

Гейт перед `published`:

- есть кластер и частотность (комбо) или уникальный проект;
- ≥4 своих кадра (комбо) / ≥3 (проект);
- заполнены уникальные поля (не общий абзац);
- внутренние ссылки на родителя и 3 соседа;
- одна H1, уникальные title/description;
- `origin` кадра проставлен;
- для комбо нет второй каноники в каталоге.

70% считаем **двумя** гейтами (не Advego «vs интернет» как единственная цифра):

1. **Данные.** Доля символов в уникальных полях + подписи к кадрам / смысловое тело (минус навигация и виджет) ≥ 0.70. Минимум 6 из 10 полей проекта реально разные у соседа (метраж, район, серия дома, год, задача, ограничения, материалы, ошибка, свои pHash-кадры, связи).
2. **Семантика.** Эмбеддинги смыслового тела (multilingual-e5 / rubert): max cosine с сиблингами того же шаблона < 0.72. Иначе `draft`.
3. Картинки: pHash без коллизий со стоком и другими карточками.

Если ниже — не публикуем, не «докручиваем водой». CI до `published`.

Редиректы синонимов: `zal`→`gostinaya`, `sovremennyj`←`contemporary`/`kontemporari`, `art-deco`→`ar-deko`. Одна карта в CMS, 301 навсегда.

## 13. Критерий «архитектура готова»

- Любой будущий URL объясняется таблицей типов и правилом индекса.
- Любая комбинация либо в белом списке, либо не существует как ЧПУ.
- Генератор можно выключить — сайт остаётся полноценной студией.
- Пилот считается в десятках URL, не в тысячах.
- Юридика и ЛК не блокируют запуск витрины: виджет можно прикрутить второй очередью.

Детализация осей: `docs/taxonomy.md`. Порядок работ: `docs/PLAN.md`. Каналы, SKU, ёмкость, CAC: `docs/gtm.md`. Жёсткий аудит пробелов (юридика, GPU, воронка, CWV): `docs/architecture-audit.md`.
